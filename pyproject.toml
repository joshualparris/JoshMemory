[project]
name = "joshmemory"
version = "0.1.0"
description = "Local searchable archive index for Codex JSONL sessions."
requires-python = ">=3.11"
dependencies = []

[project.scripts]
joshmemory = "joshmemory.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

